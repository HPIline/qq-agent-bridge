# 更新日志

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。日期格式 YYYY-MM-DD。

## [0.1.0] - 2026-01-01

第一个公开版本。QQ 私聊 AI 助手，装好填 5 行配置就能用。

### 聊天与理解

- 私聊对话，多轮上下文 + 可关闭的长期记忆
- 图片直接交给多模态模型看；拿不准的内容自动联网补查
- 视频分析：下载 → 抽帧逐帧描述 → 字幕提取 → 语音转写 → 汇总成一段"看完了"的回答
- PDF / Office 文件读取，扫描件可走 OCR（需要本机装 poppler + tesseract）
- 消息自动区分"闲聊"和"任务"：任务先回一句"收到"，再异步执行

### 干活与调度

- 大任务分阶段执行：跑一段汇报一次，等你说"继续"或"停止"
- 提醒、待办、日历、后台任务队列（本地 SQLite）
- 本地知识库：`/知识库 重建` 索引你的文档，之后 `/知识库 搜` 或让助手自己查
- 助手可以在自己的工作目录里写文件，用 `[FILE:]` 标记把成果发回 QQ

### 人格与风格

- 默认是普通中文助手；用 `persona_name` / `persona_prompt` 定义说话方式
- 也可以用 `persona_skill` 挂一个技能目录，把完整人设写在 `SKILL.md` 里
- 对话风格、表情/语音频率、主动话题规则都能用配置覆盖
- 支持自定义技能触发：命中关键词时提示模型先读某个技能

### 主动消息（默认关闭）

- 固定时间点、长时间没消息、日程开始前主动开口
- 开口前可先判断"现在适不适合打扰"；有话题冷却、每日上限、时间段限制

### 语音（默认关闭）

- 本地 GPT-SoVITS 合成语音条，可配角色与参考语气

### 接入方式

- 文件通道：别的程序写 JSON 就能让助手发 QQ 消息，QQ 消息也能落盘给对方处理
- Web 工作台：浏览器里聊天、看提醒/待办/任务/日志
- 第三方插件：兼容 AstrBot 生态里只依赖 `astrbot.api` 的简单插件

### 运行环境

- Windows / macOS / Linux 同一套代码，`start.bat` / `start.sh`
- 任意 OpenAI 兼容接口（官方、DeepSeek、中转、自建、本地 Ollama）
- 全部 180 项配置见 [CONFIG.md](CONFIG.md)，任何一项都能用 `QQBOT_*` 环境变量覆盖
- 依赖分层安装：核心 / Web / 语音 / macOS 转写 / 开发，用不到就不用装
